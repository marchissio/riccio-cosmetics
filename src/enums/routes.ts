export enum Routes {
    HOME = "/",
    ABOUT = "/about",
    SHOP = "/shop",
    BLOG = "/blog",
    PAGES = "/pages",
    CONTACT = "/contact",
}

Object.freeze(Routes);

export const PathsConstant = Routes;
