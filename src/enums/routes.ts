export enum Routes {
    HOME = "/home",
    ABOUT = "/about",
    SHOP = "/shop",
    BLOG = "/blog",
    PAGES = "/pages",
    CONTACT = "/contact",
}

Object.freeze(Routes);

export const PathsConstant = Routes;
